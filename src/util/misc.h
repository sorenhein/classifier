#ifndef TRAIN_MISC_H
#define TRAIN_MISC_H

using namespace std;


float ratioCappedUnsigned(
  const unsigned num,
  const unsigned denom,
  const float rMax);

float ratioCappedFloat(
  const float num,
  const float denom,
  const float rMax);

#endif
